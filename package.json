{
  "name": "enzyme-obsidian-plugin",
  "version": "0.1.0",
  "description": "Enzyme — surfaces connections across your vault, rendered as a clickable digest",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production"
  },
  "dependencies": {
    "obsidian": "^1.5.7"
  },
  "devDependencies": {
    "@types/node": "^20.11.30",
    "builtin-modules": "3.3.0",
    "esbuild": "0.19.11",
    "typescript": "^5.3.3"
  }
}
